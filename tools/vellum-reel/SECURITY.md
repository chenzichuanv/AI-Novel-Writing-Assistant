# Security policy

Do not report exposed credentials, signed media URLs, or personal content in a public issue. Contact the repository owner privately through GitHub instead.

Before publishing a project created with VellumReel, verify that `.env`, `books/`, `narratives/`, `public/assets/`, `out/` and provider manifests are not staged.
